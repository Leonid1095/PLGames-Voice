# План работ по итогам аудита — 2026-07-31

> **Дата аудита:** 2026-07-31 | **Ветка:** `dev` (`bdcc37ea`)
> **Объём:** безопасность, Docker/infra, i18n, качество кода и зависимости
> **Статус:** P0 закрыт (A1–A4). Из P1 сделаны B1, B2, B5, B6, C1, D1, часть B3 (cap_drop/no-new-privileges fleet-wide + redis/rabbit non-root), часть C5 (линтер бота). Открыты: B3-хвост (Dockerfile `USER` для api/events/web), B4 (egress), C2/C3/C4 (Rust deps), C5 (тесты бота). Из P3 закрыты коммитом `19719f48`: X-XSS-Protection:0, Permissions-Policy, X-Forwarded-For на /whip+/stream/, `ssl_protocols` в репо, setup-config.sh shim. Вне плана: `caddy.depends_on` — фикс /ws 502 при пересборке.

Приоритеты привязаны к серьёзности находок: **P0 = CRITICAL**, **P1 = HIGH**, **P2 = MEDIUM**, **P3 = LOW**. Все ссылки — `file:line`.

---

## P0 — CRITICAL (блокируют безопасность / деплой)

### [x] A1. Purge секретов из git-истории  ✅ ГОТОВО (verified 2026-08-01)
**Проблема:** коммит «clean history» (`7b6c3424`) не сработал. В `origin/main` и теге `v2.0.0` остались:
- VAPID EC private key — `server/crates/core/config/Revolt.toml:104` (коммит `0b124465`)
- `[files] encryption_key = "qcuMA+ss..."` — `server/crates/core/config/Revolt.toml:137`
Репо публичный (`desktop/forge.config.ts:192`).

**Задачи:**
- [x] `git filter-repo --replace-text` с правилами замены обоих ключей (плейсхолдеры `REDACTED_VAPID_PRIVATE_KEY` / `REDACTED_FILES_ENCRYPTION_KEY` стоят в 4 коммитах)
- [x] `git push --force` всех веток (`origin/main`, `origin/dev` чисты)
- [x] Перетегнуть `v2.0.0` и `v1.6.0` с чистых коммитов (теги смотрят на чистые деревья)
- [x] Проверить: `git log --all -S 'qcuMA'` → пусто; перебор всех блобов `Revolt.toml` → только `""`/`REDACTED_*`
- [x] Ключи считать скомпрометированными/disclosed (на инстансе plgames-voice.ru они переопределены, но в экосистеме — leaked)
- [ ] Уведомить клонов/пользователей о re-clone (вне кодовой базы)

### [x] A2. Починить `setup.sh` — свежий деплой не стартует  ✅ (`090fc0e4`)
**Проблема:** `setup.sh:127-157` генерирует `.env` без `REDIS_PASSWORD` и `MESSAGES_ENCRYPTION_KEY`. При этом `compose.yml:40,54` требует `REDIS_PASSWORD` (`:?`), а `Revolt.toml.template:3,37` их подставляет → Compose отказывается поднимать редис и всю зависимую цепочку.

**Задачи:**
- [ ] Добавить генерацию: `REDIS_PASSWORD` (openssl rand base64 24) и `MESSAGES_ENCRYPTION_KEY` (openssl rand base64 32) через `keep_or_make`
- [ ] Добавить обе переменные в heredoc `.env`
- [ ] Сверить финальный список с `.env.example` (там они есть) — генератор и example не должны расходиться
- [ ] Тест: чистый прогон `setup.sh` в empty dir → `docker compose config` проходит без `?`-ошибок

### [x] A3. Удалить или заменить `generate_config.sh`  ✅ (`cf82b16b`)
**Проблема:** ловушка потери данных — на каждом запуске `openssl rand` для `encryption_key` (`:45`). Плюс ломает `HOSTNAME` (`:14`), BSD-only `base64 -i` (`:38`), TURN off (`:61`).

**Задачи:**
- [ ] Заменить тело на `exec ./setup.sh "$@"` (полностью superseded) **ИЛИ** удалить файл
- [ ] Проверить: нигде в docs/scripts нет ссылок на `generate_config.sh`

### [x] A4. Убрать дамп конфига в stdout  ✅ (`b5eb995d`)
**Проблема:** `server/crates/core/config/src/lib.rs:455-459` — `println!("{:?}", config())` печатает весь `Settings` при старте, включая SMTP-учётки, хосты, URI. Утечка через `docker logs`/journald.

**Задачи:**
- [ ] Удалить println! или заменить на targeted/redacted лог (только статус: «config loaded», без секретов)
- [ ] Заодно убрать `log::info!("database_here {db:?}")` — `server/crates/delta/src/main.rs:42` (отладочный дамп)

---

## P1 — HIGH

### Безопасность / инфра

#### [x] B1. CI для security-тестов  ✅ (`1a587167`)
**Проблема:** `tests/security/secrets-scan.test.ts` существует, но нет workflow, который его запускает (`N1`).

**Задачи:**
- [ ] Создать `.github/workflows/security.yml`: на PR/push — `pnpm install && pnpm vitest run tests/security/`
- [ ] (Опц.) добавить шаг gitleaks/trufflehog для скана истории (рабочий тест не покрывает историю)

#### [x] B2. Сетевая изоляция контейнеров  ✅ (2026-08-01)
**Проблема:** все 17 сервисов в одной flat-сети; web/caddy достают до database/redis (`compose.yml`, нет top-level `networks:`).

**Задачи:**
- [x] Определить сети: `frontend` (web, caddy, bot, january) + `backend` (database, redis, rabbit, minio, crond, pushd, livekit-ingress, livekit-egress, createbuckets); api/events/autumn/voice-ingress/livekit — в обеих
- [x] `api`/`events` подключить к обеим; публичные — только к `frontend`
- [x] Проверить: `docker compose up`, все 17 сервисов healthy; runtime-тесты — caddy/web не резолвят data-stores (`bad address`), caddy→api работает
- [x] minio aliases перенесены с `default` на `backend`

**Gotcha (зафиксирован в compose.yml):** встроенный Docker-пул 172.17–172.31 был исчерпан другими проектами, и `frontend`-сеть упала в 192.168.0.0/16 → получила 192.168.16.0/20, который поглотил админ-LAN-шлюз 192.168.31.1 и **отрубил SSH к хосту** (2026-08-01). Подсеть `frontend` теперь захардкожена как `10.201.0.0/24` (внутри host-wide default-address-pools `10.192.0.0/12` из `/etc/docker/daemon.json`).

#### [~] B3. Контейнеры не от root + hardening  (частично — `b37b2882`)
**Проблема:** `api`/`events`/`web` работали как root, без `cap_drop`/`no-new-privileges`/`read_only` (`N3`).

**Готово:**
- [x] Якорь `&default-hardening` (`cap_drop: ["ALL"]` + `security_opt: ["no-new-privileges:true"]`) применён ко всем сервисам (`compose.yml:16`)
- [x] `redis` (`user: "999:1000"`) и `rabbit` (`user: "999:999"`) запускаются non-root напрямую — entrypoint-овый root-путь (`chown`+`su-exec`/`gosu`) ломался под cap_drop
- [x] Точечный возврат caps через `cap_add`: `NET_BIND_SERVICE` для caddy, нужные — для database/rabbit

**Осталось:**
- [ ] `server/Dockerfile.api`, `server/Dockerfile.events`, `client/Dockerfile` — добавить runtime `USER` non-root (api сейчас root + `group_add: ["65532"]` для чтения `Revolt.toml`, см. `compose.yml:211-215`)
- [ ] (Опц.) `read_only: true` + `tmpfs: ["/tmp"]` где возможно

#### [ ] B4. Изоляция `livekit-egress`
**Проблема:** `SYS_ADMIN` + root + flat-сеть без компенсаций (`compose.yml:388-389`).

**Задачи:**
- [ ] AppArmor/seccomp профиль
- [ ] `cap_drop: ["ALL"]` + оставить только `SYS_ADMIN`
- [ ] `security_opt: ["no-new-privileges:true"]`
- [ ] Изолировать в `backend`-сети (после B2)

#### [x] B5. nginx `server_tokens off`  ✅ (`02ec91f0`)
**Проблема:** версия nginx светится в заголовках/ошибках (`plgvoice.conf`).

**Задачи:**
- [ ] Добавить `server_tokens off;` (в `server {}` или на уровне `http`)
- [ ] `sudo nginx -t` + `sudo systemctl reload nginx`
- [ ] Синхронизировать `plgvoice.conf` → `/etc/nginx/sites-enabled/`

#### [x] B6. Права на TLS-приватники  ✅ (`02ec91f0`)
**Проблема:** `chmod 644` на `*.pem` делает `privkey.pem` world-readable (`setup.sh:199`, `bot/renew-livekit-certs.sh:10`).

**Задачи:**
- [ ] Заменить glob-`644` на раздельный: `fullchain.pem` → `644`, `privkey.pem` → `600`
- [ ] Добавить `set -euo pipefail` в `renew-livekit-certs.sh` + рестарт только если файл изменился (`cmp -s`)

### Качество кода / зависимости

#### [x] C1. Удалить мёртвый код в `delta/main.rs`  ✅ (`02ec91f0`)
**Задачи:**
- [ ] Удалить дубликат `swagger_0_8` — `server/crates/delta/src/main.rs:100-106` (shadowed, мёртвый)

#### [ ] C2. Саплай-чейн зависимости
**Задачи:**
- [ ] `server/Cargo.toml:13` — мигрировать с форка `redis` (rev-pinned 0.23.3) на upstream `redis ≥ 0.27`; убрать feature-gate `redis-is-patched` (`server/crates/core/presence/Cargo.toml:12`)
- [ ] `server/crates/delta/Cargo.toml:53` — `lettre 0.10.0-alpha.4` → `lettre 0.11` stable

#### [ ] C3. Консолидация версий крейтов
**Задачи:**
- [ ] Унифицировать `ulid` на `1.x` (сейчас 0.4/0.5/1.x в разных crates)
- [ ] Поднять все crates на `rocket = "0.5.1"` (есть `0.5.0-rc.2` в voice-ingress)
- [ ] Свести `reqwest` к `0.12` (delta 0.11, january 0.12)
- [ ] После — `cargo tree -d`, сверить сокращение дублей (сейчас 131 дублирующийся крейт)

#### [ ] C4. Обновить устаревшие библиотеки
**Задачи:**
- [ ] `server/crates/bonfire/Cargo.toml:33` — `async-tungstenite 0.17` → `0.28` (bonfire WS)
- [ ] `server/crates/delta/Cargo.toml:23` — `env_logger 0.7` → `0.11`
- [ ] Заменить `isahc 1.7` (архив) → `reqwest`
- [ ] Спланировать миграцию `async-std 1.8` (unmaintained) → `tokio`

#### [ ] C5. Бот: тесты + линтер
**Проблема:** `bot/index.js` — 2950 строк, 0 тестов, 0 линтера, 0 типов.

**Задачи:**
- [ ] Добавить ESLint-конфиг (flat, как в client)
- [ ] Покрыть критичные пути unit-тестами (XP-формула, автомод, парсеры команд)
- [ ] (Опц.) на `process.on("uncaughtException")` добавить `process.exit(1)` после лога

### i18n

#### [~] D1. Добавить 14 отсутствующих переводов  ✅ переводы (`ac653236`); npm-scripts `lingui:extract`/`lingui:compile` — проверить отдельно
**Проблема:** строки есть в исходниках, но отсутствуют в каталогах → русские пользователи видят английский.
Кластер — флоу создания сервера: `CreateServer.tsx:28-129` (9 строк), `Home.tsx:293-294` (viewer/viewers), `Reactions.tsx:139`, `EditCategory.tsx:65`, `CreateBot.tsx:62`.

**Задачи:**
- [ ] `cd client/packages/client && pnpm exec lingui extract` (добавит пустые msgstr)
- [ ] Перевести 14 строк в `components/i18n/catalogs/ru/messages.po`
- [ ] `pnpm exec lingui compile --typescript` (обновить `messages.ts`)
- [ ] Добавить npm-scripts `lingui:extract` + `lingui:compile` в `client/packages/client/package.json` (сейчас их нет, хотя CLAUDE.md ссылается на `pnpm lingui:extract`)
- [ ] Пересобрать web-образ

---

## P2 — MEDIUM

### Infra / конфиги
- [ ] `scripts/backup.sh:58-66,103` — Mongo/Redis пароли из argv (`ps`-видимы): передавать через `--uri`/`REDISCLI_AUTH` env или root-only файл
- [ ] `plgvoice.conf:4` — снизить `client_max_body_size` со `100M` до `25M` (лимит API 20 МБ — уситель расхода памяти)
- [ ] `plgvoice.conf` — добавить `limit_req_zone` + `limit_req` на `/api`/`/ws` (анти-брутфорс)
- [ ] `plgvoice.conf` — добавить `gzip on; gzip_types ...`
- [ ] `compose.yml` — CPU/PID лимиты (`cpus`, `pids_limit`) на все сервисы
- [ ] `compose.yml` — healthcheck для `bot` (`wget --spider http://127.0.0.1:3939/`), `livekit-ingress`, `livekit-egress` (они НЕ distroless)
- [x] `compose.yml` — `caddy.depends_on` (ждать `events`/`api`/`web` healthy перед стартом) — убрал /ws 502 при пересборке (вне аудита, зафиксировано 2026-08-01)
- [ ] Синхронизировать `livekit.yml.template` с live (relay_range, turn subdomain `turn.plgames-voice.ru`)
- [ ] Почистить orphan-ключи: `VITE_GIFBOX_URL` (нет роута), `VITE_TENOR_API_KEY` (не используется) — свести к Giphy
- [ ] `Revolt.toml`, `egress.yml` → режим `0600` (сейчас `0640` с секретами)
- [ ] Выровнять владельцев `data/` под uid контейнеров (`chown 999:999 data/redis data/rabbit`, и т.д.)

### Код
- [ ] `desktop/package.json:45` — `typescript ~4.5.4` → `^5.5`; `desktop/tsconfig.json` → `"strict": true`
- [ ] `desktop` — ESLint 8/typescript-eslint 5 → выровнять с client (ESLint 9/ts-eslint 8)
- [ ] `client/packages/client/package.json:150` — `zod 3.23.8` (exact patch) → `^3.25`
- [ ] Клиент UI: заложить unit-тесты (сейчас 3 e2e, 0 unit) — начать со state-stores и markdown-парсера

---

## P3 — LOW

- [x] `Caddyfile:7`, `plgvoice.conf:12` — `X-XSS-Protection: 0` (`19719f48`)
- [x] Добавить `Permissions-Policy` (camera/microphone scoped) (`19719f48`)
- [x] `plgvoice.conf:59-64,70-74` — в `/stream/` и `/whip` добавить `X-Forwarded-For`/`X-Forwarded-Proto` (`19719f48`)
- [~] Зафиксировать TLS-настройки в репо: `ssl_protocols TLSv1.2 TLSv1.3;` ✅ (`19719f48`); `ssl_ciphers` — ещё открыто (делегировано Certbot `options-ssl-nginx.conf`)
- [ ] CSP: убрать `unsafe-inline`/`unsafe-eval` (переход на nonce-based) — известный долг
- [ ] Скрипты `*.sh` — `chmod 0755` (сейчас group-writable `0775`, а внутри sudo)
- [ ] `.env.example` — все placeholder'ы привести к `changeme_*`/`<...>`, чтобы валидатор (`setup-config.sh:37`) их ловил
- [x] `setup-config.sh` — заменить на `exec setup.sh` shim (`19719f48`; был хрупкий `source <(grep…)`)
- [ ] `en/messages.po:14` — заполнить `Plural-Forms`; обновить `en` каталог (10 ghost + 24 missing)
- [ ] `components/legacy/` — спланировать вывод из эксплуатации (8 deprecated компонентов)
- [ ] `components/markdown/solid-markdown/ast-to-solid.tsx:1` — убрать blanket `eslint-disable`
- [ ] `server/deny.toml:163` — `wildcards = "deny"` вместо `allow`

---

## Метрики / критерии приёмки

| Метрика | Сейчас | Цель после работ |
|---------|--------|------------------|
| Секретов в git-истории | 2 (leaked) | 0 |
| Свежий деплой из `setup.sh` | сломан | работает |
| Дамп конфига в логах | да | нет |
| CI security-скан | нет | на каждый PR |
| Сетевых зон в compose | 1 (flat) | 2+ (frontend/backend) |
| Контейнеров от root | ~14 | 0 (кроме требуемых) |
| `server_tokens` nginx | on | off |
| Непереведённых строк (ru) | 14 | 0 |
| Дублирующих крейтов в Cargo.lock | 131 | < 60 |
| Мёртвого/отладочного кода в `delta/main.rs` | 3 блока | 0 |

---

## Порядок реализации (предложение)

```
Спринт 1 (P0):  A1 (история) → A2/A3 (деплой) → A4 (логи)        — блокеры безопасности
Спринт 2 (P1):  B1 (CI) + C1/D1 (быстрые фиксы) одним PR
                B5 (server_tokens) + B6 (права cert) — infra-quick
Спринт 3 (P1):  B2/B3/B4 (сети/root/egress) — infra-deep
Спринт 4 (P1):  C2/C3/C4 (Rust-зависимости) — отдельный PR
Спринт 5 (P1):  C5 (бот: линтер + тесты)
Далее:          P2 → P3 по backlog
```

Один PR = одна логическая задача (согласно CLAUDE.md). Перед каждым спринтом — git checkpoint.

---

*Детальные отчёты по каждой области (полные списки MEDIUM/LOW с file:line) — в выводах субагентов аудита от 2026-07-31.*
