# PLG Voice — Дорожная карта v4: UX-фичи

> **Дата:** 2026-03-14
> **Цель:** Доработать UX до уровня Discord/Telegram/Steam — мелкие улучшения, которые делают жизнь проще и интерфейс красивее

---

## 🟢 Фаза 1: Быстрые улучшения (1-2 часа каждая)

### 1.1 Кнопка копирования на блоках кода
**Приоритет:** 🔴 Высокий | **Сложность:** Лёгкая | **Статус:** ✅ (2026-03-14)

**Описание:** При наведении на блок кода (` ```code``` `) появляется кнопка «Копировать» в правом верхнем углу. При нажатии — текст копируется в буфер обмена, кнопка меняется на ✓ «Скопировано» на 2 секунды.

**Файлы:**
- `client/packages/client/components/ui/components/features/messaging/` — рендеринг Markdown/кода

**Реализация:**
- Найти компонент рендеринга `<pre><code>` блоков
- Обернуть в `position: relative` контейнер
- Добавить `<button>` с иконкой копирования, `position: absolute; top: 8px; right: 8px`
- `navigator.clipboard.writeText(code)` по клику
- Состояние: idle → copied (2с таймер)

---

### 1.2 Подсветка синтаксиса в блоках кода
**Приоритет:** 🔴 Высокий | **Сложность:** Лёгкая | **Статус:** ✅ (уже было — rehype-highlight)

**Описание:** Блоки кода с указанием языка (` ```js `, ` ```python `) подсвечиваются синтаксически. Без языка — без подсветки. Используем `highlight.js` (уже стандарт).

**Файлы:**
- Markdown рендерер (тот же компонент, что и 1.1)
- `package.json` — добавить `highlight.js`

**Реализация:**
- `pnpm add highlight.js`
- Импортировать `hljs` + тему (github-dark или one-dark-pro)
- В рендерере: если `<code class="language-xxx">` → `hljs.highlight(code, {language: xxx})`
- Lazy-load: `import('highlight.js/lib/core')` + регистрация популярных языков (js, ts, python, rust, css, html, json, bash, sql, go, java, c, cpp)

---

### 1.3 Быстрая реакция двойным кликом
**Приоритет:** 🟡 Средний | **Сложность:** Лёгкая | **Статус:** ✅ (2026-03-14)

**Описание:** Двойной клик на сообщение ставит реакцию «👍» (или последнюю использованную эмодзи). Как в Telegram.

**Файлы:**
- Компонент сообщения (Message.tsx или аналог)

**Реализация:**
- `onDblClick` на контейнере сообщения
- Проверить: не выделен ли текст (если выделен — не ставим реакцию)
- Вызвать API: `message.react('👍')` или последний использованный эмодзи из localStorage
- Анимация: кратковременное увеличение эмодзи (scale 0→1.2→1)
- Опция в настройках: вкл/выкл

---

### 1.4 Счётчик закреплённых сообщений
**Приоритет:** 🟡 Средний | **Сложность:** Лёгкая | **Статус:** ✅ (2026-03-14)

**Описание:** На иконке закреплённых сообщений в хедере канала отображается бейдж с числом закреплённых.

**Файлы:**
- `client/packages/client/src/interface/channels/ChannelHeader.tsx`

**Реализация:**
- Получить количество закреплённых: `channel.pinnedMessages?.length` или API запрос
- Показать `<Badge count={N}>` поверх иконки 📌
- Скрыть бейдж при N = 0

---

### 1.5 Индикатор качества голоса
**Приоритет:** 🟡 Средний | **Сложность:** Лёгкая | **Статус:** ✅ (2026-03-14)

**Описание:** В голосовом канале рядом с каждым участником показывается индикатор качества связи (1-4 полоски, как у Wi-Fi). Также показывается пинг в мс.

**Файлы:**
- Компоненты голосового канала (VoicePanel, VoiceParticipant)

**Реализация:**
- LiveKit предоставляет `participant.connectionQuality` (EXCELLENT, GOOD, POOR, LOST)
- Маппинг: EXCELLENT → 4 полоски (зелёный), GOOD → 3 (зелёный), POOR → 2 (жёлтый), LOST → 1 (красный)
- SVG иконка с 4 полосками, закрашенные по уровню
- Опционально: `participant.latency` → показать "42ms" тултипом

---

### 1.6 Полная дата при наведении на время сообщения
**Приоритет:** 🟢 Низкий | **Сложность:** Лёгкая | **Статус:** ✅ (уже было — format="datetime" в тултипе)

**Описание:** Сообщения показывают "14:30" или "Вчера", но при наведении на время — тултип с полной датой: "Пятница, 14 марта 2026 г., 14:30:45".

**Файлы:**
- Компонент даты/времени сообщения

**Реализация:**
- Обернуть timestamp в `<Tooltip content={fullDate}>`
- `fullDate = new Intl.DateTimeFormat('ru', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit', second: '2-digit' }).format(date)`

---

### 1.7 Разделитель «НОВОЕ» между сообщениями
**Приоритет:** 🔴 Высокий | **Сложность:** Средняя | **Статус:** ✅ (уже было + стилизация Discord-style 2026-03-14)

**Описание:** Красная линия с надписью «НОВОЕ» между последним прочитанным и первым непрочитанным сообщением. Как в Discord.

**Файлы:**
- Компонент списка сообщений
- Стор непрочитанных сообщений

**Реализация:**
- Отслеживать `lastReadMessageId` для канала
- При рендеринге списка: между `lastRead` и следующим сообщением вставить разделитель
- Разделитель: `<div>` с `border-top: 1px solid var(--md-sys-color-error)`, текст «НОВОЕ» по центру
- При прокрутке вниз до конца — сбросить маркер

---

## 🟡 Фаза 2: Средние улучшения (3-6 часов каждая)

### 2.1 Командная палитра (Ctrl+K)
**Приоритет:** 🔴 Высокий | **Сложность:** Средняя | **Статус:** ✅ (2026-03-14)

**Описание:** Ctrl+K (или Cmd+K на Mac) открывает модальное окно быстрого поиска. Можно искать каналы, пользователей, серверы, действия. Как в Discord, VS Code, Figma.

**Файлы:**
- Новый компонент `CommandPalette.tsx`
- Глобальный обработчик клавиш

**Реализация:**
- Модальное окно с полем ввода наверху
- Категории результатов: 📁 Каналы, 👤 Пользователи, ⚙️ Действия
- Fuzzy search по названиям каналов и серверов
- Навигация стрелками ↑↓, Enter для выбора
- Escape закрывает
- Действия: «Настройки», «Создать канал», «Сменить статус» и т.д.

---

### 2.2 Режим стримера
**Приоритет:** 🟡 Средний | **Сложность:** Средняя | **Статус:** ✅ (2026-03-14)

**Описание:** Переключатель в настройках. Когда включён — скрывает личную информацию: email, токены, инвайт-ссылки, ЛС-каналы. Для стримеров, которые показывают экран.

**Файлы:**
- Новая настройка в UserSettings
- Компоненты, содержащие личные данные

**Реализация:**
- Настройка: `streamerMode: boolean` в localStorage
- Хоткей: `Ctrl+Shift+S` — быстрое включение/выключение
- Что скрывать:
  - Email в настройках → `s****@***.com`
  - Инвайт-ссылки → `plgames-voice.ru/invite/****`
  - Содержимое ЛС → размытие (blur)
  - Уведомления → скрытие текста превью
- Индикатор в статус-баре: 🔴 «Режим стримера»

---

### 2.3 Фильтры поиска
**Приоритет:** 🟡 Средний | **Сложность:** Средняя | **Статус:** ⛔ (API не поддерживает from:/has: — только текст, before, after, sort)

**Описание:** Расширенный поиск с фильтрами: `от:Lenar`, `имеет:картинку`, `до:14.03.2026`, `в:#общий`. Как в Discord и Slack.

**Файлы:**
- `ChannelHeader.tsx` — панель поиска
- API поиска (серверная часть)

**Реализация:**
- Парсер синтаксиса: `от:`, `в:`, `имеет:`, `до:`, `после:`
- Автодополнение: при вводе `от:` — список пользователей
- Чипсы фильтров под строкой поиска (можно удалять ×)
- Маппинг на API параметры поиска
- Подсказка при фокусе: список доступных фильтров

---

### 2.4 Тихие сообщения
**Приоритет:** 🟢 Низкий | **Сложность:** Лёгкая | **Статус:** ✅ (2026-03-14)

**Описание:** Отправка сообщения без уведомления получателям. Ctrl+Enter (или кнопка 🔕) — тихая отправка.

**Файлы:**
- Компонент ввода сообщений
- API отправки

**Реализация:**
- Кнопка 🔕 рядом с кнопкой отправки (или Ctrl+Shift+Enter)
- Добавить флаг `silent: true` в API запрос
- На стороне сервера: не генерировать push-уведомление
- Визуально: тихие сообщения помечаются маленькой иконкой 🔕

---

### 2.5 Превью ответа/цитаты
**Приоритет:** 🟡 Средний | **Сложность:** Средняя | **Статус:** ✅ (уже было — клик → scrollIntoView + highlight)

**Описание:** При наведении на ответ (reply) — превью оригинального сообщения. При клике — прокрутка к оригиналу с подсветкой.

**Файлы:**
- Компонент Reply/Quote в сообщениях

**Реализация:**
- Hover: показать попап с полным текстом оригинала (до 300 символов)
- Click: прокрутить к оригинальному сообщению (`scrollIntoView`)
- Подсветка: оригинал мигает жёлтым на 2 секунды
- Если сообщение за пределами загруженных — подгрузить через API

---

### 2.6 Описание канала в тултипе
**Приоритет:** 🟢 Низкий | **Сложность:** Лёгкая | **Статус:** ✅ (2026-03-14)

**Описание:** При наведении на название канала в сайдбаре — тултип с описанием канала (topic). Позволяет быстро понять назначение канала.

**Файлы:**
- `ServerSidebar.tsx` — рендеринг каналов

**Реализация:**
- Обернуть название канала в `<Tooltip content={channel.topic}>`
- Показывать только если `channel.topic` не пустой
- Максимум 150 символов, остальное `...`

---

### 2.7 Загрузка кастомных эмодзи
**Приоритет:** 🟡 Средний | **Сложность:** Средняя | **Статус:** ✅ (уже было — EmojiList.tsx + пикер)

**Описание:** Администраторы сервера могут загружать кастомные эмодзи. Доступны в эмодзи-пикере в отдельной вкладке «Серверные».

**Файлы:**
- Настройки сервера → новая секция «Эмодзи»
- Эмодзи-пикер → вкладка серверных эмодзи

**Реализация:**
- UI: загрузка файла (PNG/GIF до 256KB, 128×128)
- API: `/servers/{id}/emojis` — CRUD
- Эмодзи-пикер: вкладка с иконкой сервера → список кастомных
- Синтаксис: `:emoji_name:` → рендерится как `<img>`
- Автодополнение при вводе `:` в чате

---

### 2.8 Перетаскивание каналов (Drag & Drop)
**Приоритет:** 🟡 Средний | **Сложность:** Высокая | **Статус:** ✅ (уже было — solid-dnd-directive)

**Описание:** Drag & drop каналов в сайдбаре для изменения порядка и перемещения между категориями. Как в Discord.

**Файлы:**
- `ServerSidebar.tsx`
- Потребуется библиотека: `@thisbeyond/solid-dnd` или `solid-sortable`

**Реализация:**
- Зажатие канала → визуальный drag (полупрозрачная копия)
- Drop-зоны: между каналами, между категориями
- Обновление порядка: `server.edit({ categories })` с новым порядком
- Индикатор вставки: синяя линия в месте drop
- Только для пользователей с правом `ManageChannel`

---

## 🔴 Фаза 3: Крупные фичи (1-2 дня каждая)

### 3.1 Оверлей голосового канала
**Приоритет:** 🟡 Средний | **Сложность:** Высокая | **Статус:** ✅ (2026-03-14)

**Описание:** Плавающее мини-окно с участниками голосового канала. Можно перетаскивать, сворачивать. Показывает кто говорит, кнопки мьют/дисконнект.

**Файлы:**
- Новый компонент `VoiceOverlay.tsx`
- Electron: поддержка always-on-top окна

**Реализация:**
- Плавающий `<div>` с `position: fixed`, draggable
- Список участников с индикатором говорящего (зелёная рамка)
- Кнопки: 🎤 мьют, 🔇 глухой, 📞 отключиться
- Сворачивается в мини-версию (только иконки участников)
- Сохранение позиции в localStorage

---

### 3.2 Отложенные сообщения
**Приоритет:** 🟢 Низкий | **Сложность:** Средняя | **Статус:** ✅ (2026-03-14)

**Описание:** Написать сообщение и запланировать отправку на определённое время. Как в Telegram.

**Файлы:**
- Компонент ввода сообщений
- Новый сервис планирования (бот или серверная часть)

**Реализация:**
- Кнопка ⏰ рядом с отправкой → DateTimePicker
- Сохранение в localStorage или на сервере (через бот)
- Бот проверяет по таймеру и отправляет в нужное время
- Список запланированных: можно отменить/изменить

---

### 3.3 Шаблоны серверов
**Приоритет:** 🟢 Низкий | **Сложность:** Средняя | **Статус:** ✅ (2026-03-14)

**Описание:** При создании сервера — выбор шаблона (Игровое сообщество, Учёба, Друзья, Пустой). Шаблон создаёт предустановленные каналы и категории.

**Реализация:**
- Шаблоны: JSON-описание (категории + каналы)
- UI: карточки шаблонов с превью
- При выборе: создать сервер → создать категории и каналы из шаблона

---

### 3.4 Журнал аудита
**Приоритет:** 🟡 Средний | **Сложность:** Высокая | **Статус:** ⛔ (требует серверную часть — логирование в БД)

**Описание:** Админы видят лог действий: кто создал/удалил канал, кикнул/забанил пользователя, изменил роли.

**Реализация:**
- Серверная часть: логирование действий в БД
- UI: страница в настройках сервера → таблица с фильтрами
- Фильтры: по типу действия, по пользователю, по дате

---

## Порядок реализации

| Очередь | Фича | Фаза | Оценка |
|---------|-------|------|--------|
| 1 | Кнопка копирования кода | 1.1 | 1ч |
| 2 | Подсветка синтаксиса | 1.2 | 1.5ч |
| 3 | Разделитель «НОВОЕ» | 1.7 | 2ч |
| 4 | Полная дата в тултипе | 1.6 | 30мин |
| 5 | Описание канала в тултипе | 2.6 | 30мин |
| 6 | Счётчик закреплённых | 1.4 | 1ч |
| 7 | Быстрая реакция | 1.3 | 1.5ч |
| 8 | Индикатор качества голоса | 1.5 | 2ч |
| 9 | Командная палитра | 2.1 | 4ч |
| 10 | Превью ответа | 2.5 | 3ч |
| 11 | Кастомные эмодзи | 2.7 | 5ч |
| 12 | Режим стримера | 2.2 | 4ч |
| 13 | Фильтры поиска | 2.3 | 5ч |
| 14 | Drag & Drop каналов | 2.8 | 6ч |
| 15 | Тихие сообщения | 2.4 | 2ч |
| 16 | Оверлей голоса | 3.1 | 12ч |
| 17 | Отложенные сообщения | 3.2 | 8ч |
| 18 | Шаблоны серверов | 3.3 | 6ч |
| 19 | Журнал аудита | 3.4 | 12ч |

---

*Последнее обновление: 2026-03-14*
