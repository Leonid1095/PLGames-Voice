{
  "name": "plg-voice-security-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Security/regression tests (secrets scan, etc.)",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
