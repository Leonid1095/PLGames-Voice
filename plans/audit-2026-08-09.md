# Аудит — 2026-08-09

> **Ветка:** `dev` (`f23eade2`) | **Предыдущий аудит:** [`audit-2026-07-31.md`](audit-2026-07-31.md)
> **Охват:** прод-состояние, регрессии по закрытым пунктам, зависимости (Rust + npm), код бота и API, инфра
> **Кода не менялось с 2026-08-01** — поэтому упор на (а) проверку того, что заявленное как «сделано» реально работает в проде, и (б) области, которые прошлый аудит не покрывал: Rust-зависимости и авторизация в боте.

## Сводка

| | Находка | Статус |
|---|---|---|
| **H1** | 21 коммит харденинга не запушен — работа существует только на этой машине | ✅ закрыто 08-09 |
| **H2** | Уязвимый JPEG XL декодер в пути загрузки файлов (jxl-oxide 0.8.1 / 0.12.5) | ✅ закрыто 08-09 — autumn собран локально и развёрнут |
| **H3** | `isAdmin()` бота = «есть любая роль» — 34 из 38 команд | ✅ закрыто 08-09 |
| **H4** | Rust-дерево никогда не сканировалось: 37 уязвимых крейтов, нет CI-шага | ✅ закрыто 08-09 — CI + разбор всех 18 с обоснованиями |
| **H5** | **Реестры образов недоступны: ghcr.io не резолвится, Docker Hub CDN таймаутит** | **новое** — autumn снят с ghcr.io, остальные 5 сервисов открыты |
| **H6** | **Сборка web негерметична: `.dockerignore` пропускал вложенные `node_modules`** | **новое, найдено в ходе работ** — ✅ исправлено |
| **M1** | nginx: конфиг в проде от 24 июля + security-заголовки терялись на всей статике | ✅ закрыто 08-09 |
| **M2** | ~~`rustls-webpki` 0.101.7/0.102.8~~ — старые ветки не на пути к недоверенным пирам | ✅ понижено до LOW, проверено |
| **M3** | Source maps отдаются в проде (11.5 МБ) | ✅ закрыто 08-09 |
| **M4** | ~~Нет rate limiting на `/api`~~ — **ошибка аудита**, лимитер работает | ✅ снято, понижено до LOW |
| **M5** | `member_fetch_all.rs:43` — panic по запросу + неверная пара member↔user | ✅ закрыто 08-09 |
| **M6** | Electron 38.1.2 + 49 уязвимостей в runtime-дереве desktop | 🔶 дерево очищено (49 → 0), Electron 39.8.10 — **нужен smoke-тест до релиза** |
| **M7** | `mdast-util-to-hast` 13.2.0 — unsanitized class attribute из любого сообщения | ✅ закрыто 08-09 (после L0) |
| **H7** | **Сборка desktop для macOS не проходила ни разу с апреля** | **новое (08-10)** — ✅ обе причины устранены |
| **L0–L8** | см. ниже | ✅ L0, L1, L2, L3, L4, L6 закрыты; L5, L7, L8 — ниже |

**Что проверено и оказалось в порядке:** все 17 сервисов up (api/events/web/caddy/database/redis/rabbit/minio/livekit — healthy); бэкапы отработали сегодня в 04:00, все 5 артефактов на месте, ретенция 60 дней держится; TLS-сертификат `plgames-voice.ru` валиден до 2026-10-06 (58 дней); TLS 1.0/1.1 **отклоняются** (глобальный `ssl_protocols` в `nginx.conf:23`); `/stream/active` без сессии отдаёт 401 — фикс `41c9c657` держится; секреты в истории git чисты (`git log --all -S` пусто), `.env`/`Revolt.toml`/`livekit.yml`/`egress.yml`/`ingress.yml` не трекаются и права на них корректные; secrets-scan тест 4/4 зелёный; ESLint бота чистый; ru-каталог — 2 непереведённых из 1071; лимиты памяти реально применены (проверено `docker inspect`, а не по yaml) — **но по CPU этот вывод оказался неверным, см. L1**; npm audit бота — 0 находок; firewall (ufw) закрывает всё лишнее.

---

## HIGH

### H1. 21 коммит не запушен; ни одна ветка не имеет upstream

```
$ git rev-list --count origin/dev..dev   →  21
$ git rev-parse --abbrev-ref dev@{upstream}
fatal: no upstream configured for branch 'dev'
$ git for-each-ref --format='%(refname:short) -> %(upstream:short)' refs/heads/
dev ->            # пусто у всех 10 веток
```

Последний `git fetch` — 2026-08-01 10:11. **Вся работа аудита 2026-07-31 (A1–A4, B1–B6, C1–C4, D1) живёт только на этой машине.** Прямые следствия:

- **CI ни разу не видел эти коммиты.** `.github/workflows/security.yml` триггерится на `push: [main, dev]` — но пушей не было, значит secrets-scan и bot-lint на этой работе не прогонялись ни разу (локально я прогнал — зелёные, но это не то же самое).
- Диск умирает → пропадает харденинг, purge истории, консолидация зависимостей.
- Это **буквальный повтор** проблемы, которую закрывал аудит 2026-07-24 («stoat.js local-only commits», 12 коммитов на одной машине). Тогда исправили симптом, но не причину — отсутствие upstream у веток.

**Задачи:**
- [ ] `git push -u origin dev` (и остальные живые ветки — `main`, `feat/polden-redesign`, `checkpoint/pre-hardening-20260801`)
- [ ] Проверить, что CI прошёл на GitHub
- [ ] `git config --global push.autoSetupRemote true` — чтобы `git push` на новой ветке сам ставил upstream

---

### H2. Уязвимый JPEG XL декодер в пути загрузки файлов

`autumn` пропускает **каждый загружаемый файл** с `image/jxl` через jxl-oxide:

- `server/crates/services/autumn/src/metadata.rs:54-60` — `jxl_oxide::JxlImage::builder().read(reader)` на этапе извлечения метаданных
- `server/crates/core/files/src/implementation/media_impl.rs:65,117` — декодирование для превью
- `image/jxl` явно в списке принимаемых типов (`metadata.rs:16`)

**Две разные версии в дереве**, потому что autumn пинит свою в обход workspace:

| Место | Версия | |
|---|---|---|
| `server/Cargo.toml:39` | `jxl-oxide = "0.12.5"` | workspace |
| `crates/core/files/Cargo.toml:35` | workspace → 0.12.5 | |
| `crates/services/autumn/Cargo.toml:16` | **`jxl-oxide = "0.8.1"`** | хардкод мимо workspace |

Обе затронуты:

- **GHSA-66m8-c62j-h6v5** (`jxl-oxide`, fixed ≥ **0.12.6**) — `FrameBuffer::new` строит **out-of-bounds slice** при переполнении. CVSS `AV:L/.../A:H`.
- **GHSA-2v8p-fqpx-2q3w** (`jxl-modular`, fixed ≥0.11.3; у нас 0.7.1 и 0.11.2) — переполнение вычитания в `cluster_from_table` на crafted JXL → panic/DoS.

Нюанс сборки: `server/Cargo.toml:17-18` — `[profile.release] lto = true`, `overflow-checks` по умолчанию **выключены** в release. То есть переполнение из GHSA-2v8p не паникует, а **заворачивается** — и завёрнутое значение это ровно тот путь, который в GHSA-66m8 приводит к построению out-of-bounds slice. Обе записи оценены как impact на доступность (падение/DoS), не как RCE, — но триггер — обычная загрузка файла любым пользователем.

Тот же класс — SVG: `usvg`/`resvg 0.44.0` тянут `rustybuzz 0.18.0` (RUSTSEC-2026-0206), `ttf-parser 0.24.1` (RUSTSEC-2026-0192, unmaintained), `memmap2 0.9.10` (RUSTSEC-2026-0186) — тоже на пользовательских загрузках.

**Задачи:**
- [ ] `crates/services/autumn/Cargo.toml:16` → `jxl-oxide = { workspace = true }` (убрать вторую версию из дерева)
- [ ] `server/Cargo.toml:39` → `jxl-oxide = "0.12.6"` (минимум) с `features = ["image"]`
- [ ] Пересобрать `autumn` + сервисы, тянущие `revolt-files`; проверить, что jxl-превью не сломались (`crates/core/files/tests/assets/dice.jxl`, `anim-icos.jxl` — тесты уже есть в `media_impl.rs:263,274`)
- [ ] Отдельно оценить бамп `usvg`/`resvg` 0.44 → свежая ветка

---

### H3. `isAdmin()` бота = «у пользователя есть хоть одна роль»

`bot/index.js:250-261`:

```js
async function isAdmin(serverId, userId) {
  try {
    const member = await api("GET", `/servers/${serverId}/members/${userId}`);
    const server = await api("GET", `/servers/${serverId}`);
    if (server.owner === userId) return true;
    // Check if member has any roles (basic permission check)
    return member.roles && member.roles.length > 0;   // ← вот это
  } catch { return false; }
}
```

`grep -c "isAdmin(SERVER_ID" bot/index.js` → **34** из 38 команд: `kick`, `ban`, `unban`, `mute`, `purge`, выдача XP, управление турнирами, `stream start/stop`, автомод. Все они открываются любому, кому выдали **любую** роль — включая чисто косметическую (цвет ника, «Стример», «Новичок»).

**Текущая экспозиция низкая**: в живой БД существуют только роли `Admin` / `Адмнистратор`, обе с полным набором прав, и назначена ровно одна. То есть прямо сейчас «есть роль» ≈ «админ». Но это ловушка, которая срабатывает молча в момент создания первой декоративной роли — а роли и уровни есть в роадмапе.

API отдаёт реальные права: у роли в БД лежит `permissions.a` (`{"high":127,"low":-1032225}`). Проверять нужно нужный бит (`KickMembers` / `BanMembers` / `ManageMessages`), либо завести явный allowlist ролей через env.

**Задачи:**
- [ ] Заменить «есть роли» на проверку бита прав либо `ADMIN_ROLE_IDS` из env
- [ ] Развести уровни: модерация (kick/ban/mute/purge) vs утилиты (XP/турниры/стримы) — сейчас всё под одним чеком
- [ ] Покрыть `isAdmin` юнит-тестом (см. L7 — тестов у бота 0)

---

### H4. Rust-дерево ни разу не сканировалось на уязвимости

`cargo-audit` на машине не установлен; в `.github/workflows/security.yml` нет ни `cargo audit`, ни `pnpm audit`. Прогнал `Cargo.lock` (993 крейта) через OSV batch API — **37 уязвимых крейтов**.

Разбор по достижимости (обратный граф построен из `Cargo.lock`):

**Достижимо от пользовательского ввода:**
- `jxl-oxide` / `jxl-modular` / `jxl-grid` ← `revolt-autumn`, `revolt-files` → **H2**
- `rustls-webpki 0.101.7` ← `rustls 0.21.12`; `0.102.8` ← `rustls 0.22.4` → **M2**
- `idna 0.2.3 / 0.3.0 / 0.4.0` ← `validator 0.15.0 / 0.16.1` — RUSTSEC-2024-0421: принимает Punycode-метки, не дающие non-ASCII при декодировании (спуфинг домена в валидации)
- `rustybuzz` / `ttf-parser` / `memmap2` ← `usvg 0.44.0` ← `revolt-files` (SVG-превью)

**В дереве, но путь не задействован:**
- `rsa 0.7.2` ← `jwt-simple 0.11.9` и `rsa 0.9.10` ← `jsonwebtoken 10.4.0` — RUSTSEC-2023-0071 (Marvin, тайминг-атака на восстановление ключа). LiveKit-токены подписываются HMAC, RSA-ветка компилируется, но не используется.
- `lettre 0.10.4` ← **`authifier 1.0.16`** — RUSTSEC-2026-0141 + GHSA-4pj9-g833-qx53. ⚠️ План `audit-2026-07-31.md` (C2) записал lettre как удалённую — это относилось только к **прямой** зависимости `delta`. Крейт по-прежнему в дереве и по-прежнему тот, через который реально уходит почта.
- `tokio 0.2.25` (6 advisories) ← `async-global-executor 2.4.1` ← async-std. Ещё один аргумент за C4 (async-std → tokio).
- `yaml-rust 0.4.5` ← `config 0.13.4` — unmaintained; конфиг у нас TOML, YAML-ветка мёртвая.
- `git2` / `libgit2-sys` ← `vergen 7.5.1` — только build-time.
- `lru 0.11.1` ← `revolt-database` — RUSTSEC-2026-0002 (нарушение Stacked Borrows, UB под Miri); практического эффекта нет, но крейт наш собственный прямой.

Прочее (`anyhow`, `crossbeam-epoch`, `event-listener`, `bincode`, `paste`, `proc-macro-error`, `fxhash`, `instant`, `cmov`, `hickory-proto`, `serde_with`, `rustls-pemfile`, `async-std`) — в основном «unmaintained»/информационные.

**Задачи:**
- [x] Добавлены jobs `cargo audit` и `pnpm audit --prod` в `security.yml` (`fe943ab0`). Оба `continue-on-error` — с 37 непроработанными находками блокирующая джоба была бы красной с первого дня и её бы просто игнорировали. Проверено на реальном ране: итог рана `success`, обе advisory-джобы отчитались красным отдельно.
- [x] **Базовая линия зафиксирована**: `cargo audit` на 982 крейтах → **18 уязвимостей + 23 предупреждения** (unmaintained). Столбик OSV в 37 крейтов считает крейты, cargo-audit — advisories; часть крейтов несёт по несколько.
- [x] **Разобрано полностью. 18 → 0 уязвимостей.** Починено бампом: `jxl-oxide` 0.12.6 (H2), `crossbeam-epoch` 0.9.20. Остальные — с письменным обоснованием недостижимости и названным триггером перепроверки: idna (validator делает только length/regex), lettre (Boring TLS не собирается, у нас native-tls), libgit2-sys (build-time через vergen), rsa (подписи ES256/HMAC, RSA-операции не выполняются), rustls-webpki ×4 (нет пути к недоверенному TLS-пиру), hickory ×2 (только для `mongodb+srv://`, у нас обычный URI), tokio 0.2 (честная отсрочка до C4).
- [x] **`continue-on-error` снят с Rust-джобы — она блокирующая.** Подтверждено зелёным прогоном на `773b10a4`: «23 allowed warnings found», уязвимостей нет. npm-джоба остаётся информационной, пока не бампнута build-time цепочка lingui/babel в `client/` (6 находок, все там).
- Обоснования живут в `deny.toml`; `.cargo/audit.toml` держит голые id со ссылками на них — cargo-audit и cargo-deny читают разные файлы, а CI запускает первый.
- [ ] Поправить формулировку C2 в `audit-2026-07-31.md` — lettre не удалена, а осталась под authifier
- [ ] Побочно: локальный `cargo 1.75` **не может разобрать `Cargo.lock` v4** (`lock file version 4 requires -Znext-lockfile-bump`) — проект требует 1.92 (`server/rust-toolchain.toml`, все Dockerfile на `rust:1.92.0`). Локально `cargo tree`/`cargo audit` не работают вообще. Поставить 1.92 через rustup.

---

### H5. Реестры образов недоступны — половина сервисов невоспроизводима

Найдено при попытке доставить фикс H2. Хост ходит в GitHub и Docker Hub через прокси `147.90.15.214` (свой, сертификат Let's Encrypt на `*.plgames-wow.online`, там же `proxy-1`/`stealth-1`). Прокси разбирает по SNI и покрывает не всё:

| Хост | Резолвится в | Сертификат при SNI | Результат |
|---|---|---|---|
| `github.com` | 147.90.15.214 | `CN=github.com` | ✅ `git push` работает |
| `registry-1.docker.io` | 147.90.15.214 | `CN=*.docker.com` | ⚠️ манифесты идут, блобы — нет |
| `ghcr.io` | 147.90.15.214 | `CN=api.plgames-wow.online` (дефолтный вхост) | ❌ `x509: certificate is valid for ... not ghcr.io` |

**Следствие 1 — 6 сервисов невозможно восстановить.** Из ghcr.io тянутся `autumn`, `january`, `crond`, `pushd`, `voice-ingress`, `livekit`. Сейчас они работают на образах, лежащих на диске, но перетянуть их нельзя. Это ровно та проблема, которую закрывал аудит 2026-07-24 («шесть сервисов ссылались на образы, которых нигде нет») — другая причина, тот же результат.

**Следствие 2 — сборка web стала лотереей.** Проверено на практике: `docker compose up -d --build web` упал на `FROM node:24-alpine` с `TLS handshake timeout` на `production.cloudfront.docker.com`. Локальной копии не было — недельный `docker system prune -af --filter until=168h` (вс, 04:00, сегодня отработал) вычистил её как неиспользуемую. Прямой `docker pull node:24-alpine` тоже упал с первого раза и прошёл со второго, то есть **блоб-CDN Docker Hub флапает, а не заблокирован**: сборка проходит, но с ретраями и не с первой попытки. Для ghcr.io всё жёстче — там отказ стопроцентный и воспроизводимый.

**Задачи:**
- [ ] Добавить вхост `ghcr.io` на прокси 147.90.15.214 (машина не эта — нужен доступ владельца)
- [ ] Разобраться с блоб-CDN Docker Hub (`production.cloudfront.docker.com`) — он в проксировании не участвует
- [ ] До починки: исключить базовые образы из недельного prune (`--filter label!=...` либо явный `docker pull` в cron), иначе следующий prune так же выбьет `rust:1.92.0-slim-bookworm` и сборка api тоже встанет
- [ ] Рассмотреть локальное зеркало/`registry:2` pull-through cache для ghcr.io и docker.io

---

## MEDIUM

### M1. nginx: в проде конфиг от 24 июля, правки из репо не развёрнуты

```
/etc/nginx/sites-enabled/plgvoice.conf   — mtime 24 июля 19:18
```

`diff` репо ↔ прод показывает, что коммиты `19719f48` и `02ec91f0` (помеченные в плане как ✅) на nginx **не приехали**. Разошлись:

| Правка | В репо | В проде | Реально действует? |
|---|---|---|---|
| `server_tokens off` | есть | нет | **да** — глобально из `nginx.conf:18` |
| `ssl_protocols TLSv1.2 TLSv1.3` | есть | нет | **да** — глобально из `nginx.conf:23`; TLS 1.0/1.1 отклоняются (проверено handshake'ом) |
| `X-XSS-Protection: 0` | есть | `1; mode=block` | **нет** |
| `Permissions-Policy` | есть | нет | **да** — приходит от Caddy |
| `X-Forwarded-For` / `-Proto` на `/stream/`, `/whip` | есть | нет | **нет** |

Живой ответ содержит **задвоенные security-заголовки** — nginx добавляет свой набор поверх Caddy'шного:

```
x-xss-protection: 0                 ← Caddy
x-xss-protection: 1; mode=block     ← nginx (старый файл)
strict-transport-security: ...      ×2
x-frame-options: DENY               ×2
content-security-policy: ...        ×2
```

Практический риск от самого дублирования близок к нулю (CSP одинаковые и складываются по пересечению; XSS Auditor выпилен из всех современных браузеров). Реальная проблема — **процессная**: файл в репо и файл в проде разъехались, а трекер считает пункты закрытыми. Плюс один содержательный дефект: `/stream/` в проде отдаёт бэкенду только `X-Real-IP`, без `X-Forwarded-For`/`-Proto`.

**Найдено при починке — заголовки терялись на всей статике.** nginx наследует `add_header` во вложенный блок **только если тот не объявляет ни одного своего**. Локейшен статики (`plgvoice.conf:87-92`) объявляет `add_header Cache-Control` → все 7 security-заголовков не применялись **ни к одному `.js`, `.css`, `.png`, `.svg`, `.wasm`**. В проде это было замаскировано ровно тем дублированием, которое мы собирались убрать: заголовки на статику ставил Caddy. Снять дубли, не заметив этого, означало оставить всю статику голой.

**Задачи:**
- [x] `sudo cp plgvoice.conf /etc/nginx/sites-enabled/ && nginx -t && systemctl reload nginx` (`0592747e`)
- [x] Владелец заголовков — nginx: он единственный хоп, через который проходит **всё** (`/whip` и `/stream/` идут мимо Caddy). У Caddy набор снят, оставлен только `-Server`
- [x] Продублировать набор в локейшене статики с комментарием, почему его нельзя унаследовать
- [x] Проверено вживую: 7 заголовков на HTML, по одному каждого; те же 7 теперь есть на `/assets/index-*.js`; `/`, `/api/`, `/autumn/`, `/january/` → 200; `/stream/active` → 401; `/stream/livekit-client.js` → 200

### M2. `rustls-webpki` — 2 CVE в валидации сертификатов

`0.101.7` (← rustls 0.21.12) и `0.102.8` (← rustls 0.22.4), обе ветки живут параллельно со свежей `0.23.40`:

- **RUSTSEC-2026-0098** — name constraints для URI-имён принимались некорректно (fixed ≥0.103.12)
- **RUSTSEC-2026-0104** — достижимая panic при разборе CRL (fixed ≥0.103.13)
- плюс GHSA-82j2-j2ch-gfr8, GHSA-965h-392x-2mh5, GHSA-xgp8-3hg3-c2mh, RUSTSEC-2026-0049/0099

Затрагивает исходящий TLS: MinIO/S3, push-доставка, и главное — **january, который ходит по произвольным пользовательским URL** для превью ссылок.

**Кто именно держит старые ветки** (обратный граф по `Cargo.lock`):

| Ветка | Держатель | Через что | В каком сервисе |
|---|---|---|---|
| rustls **0.21.12** | `aws-smithy-http-client 1.1.12` | `aws-sdk-s3` ← `revolt-files` | autumn, january, crond |
| rustls **0.21.12** | `hyper-rustls 0.24.2` ← `yup-oauth2 8.3.2` | `fcm_v1 0.3.0` | pushd |
| rustls **0.22.4** | `revolt_a2 0.10.1` (Apple push) | прямая зависимость | pushd |
| rustls **0.22.4** | `hyper-rustls 0.26.0`, `yup-oauth2 8.3.2` | `fcm_v1 0.3.0` | pushd |

Два независимых узла:

1. **`aws-smithy-http-client` тянет обе ветки сразу** — в его записи в локе одновременно `hyper 0.14` + `rustls 0.21.12` и `hyper 1.9` + `rustls 0.23.40`. Старая почти наверняка за legacy-фичей, которую включает `aws-config`. Вероятный фикс — `default-features = false` на `aws-config`/`aws-sdk-s3` в `crates/core/files/Cargo.toml:26-27` с явным набором фич. **Это единственный из четырёх путей, который после локальной сборки autumn реально доедет до прода.**
2. **`fcm_v1 0.3.0` и `revolt_a2 0.10.1`** — оба живут только в `pushd`, а pushd идёт pulled-образом (H5). Даже пропатчив лок, в прод это не попадёт, пока pushd не начнёт собираться локально. Сначала H5.

**Проверка достижимости (2026-08-09) — понижено до LOW, хирургия отменена.** Обе записи (некорректный разбор name constraints, паника на CRL) требуют **враждебного TLS-пира**. Ни один компонент на старых ветках такого не встречает:

| Путь | Ветка rustls | С кем говорит | Достижимо? |
|---|---|---|---|
| S3 (`aws-sdk-s3` ← autumn/january/crond) | 0.21.12 | `Revolt.toml [files.s3] endpoint = "http://minio:9000"` — **чистый HTTP по внутренней сети** | **нет, TLS вообще не задействован** |
| pushd → FCM (`fcm_v1` → `yup-oauth2`) | 0.21/0.22 | Google | нет — доверенный пир |
| pushd → APNs (`revolt_a2`) | 0.22 | Apple | нет — доверенный пир |
| **january → произвольные пользовательские URL** | **0.23.40 (пропатчена)** | кто угодно | единственный опасный путь — **и он уже на исправленной ветке** (`reqwest 0.12.28`) |

То есть единственный компонент, который реально ходит по недоверенным адресам (january, превью ссылок), использует пропатченную 0.23.40. Старые ветки только компилируются.

Задумывавшийся фикс — `default-features = false` на `aws-sdk-s3` со сбросом фичи `rustls` (она разворачивается в `aws-smithy-http-client/legacy-rustls-ring` → `hyper-014` + rustls 0.21) — **отменён осознанно**: это хирургия над крейтом, через который проходит хранение всех пользовательских файлов, ради недостижимой записи.

- [ ] (низкий приоритет) Ветки уйдут сами при бампе `aws-config`/`aws-sdk-s3`, `fcm_v1`, `revolt_a2` — сделать попутно, не отдельным риском
- [ ] Если `[files.s3] endpoint` когда-нибудь станет `https://` к внешнему провайдеру — **пересмотреть немедленно**, тогда путь становится достижимым

### M3. Source maps в проде (открыто с 2026-07-24)

```
$ curl -o /dev/null -w "%{http_code} %{size_download}" https://plgames-voice.ru/assets/index-C52neWCi.js.map
200 11580583
```

11.5 МБ, полный исходник клиента читается публично. Фикс — `build.sourcemap: false` в `vite.config` либо не копировать `.map` в Dockerfile. Висит 16 дней.

### ~~M4. Нет rate limiting на `/api`~~ — ОШИБКА АУДИТА, понижено до LOW

**Первоначальный вывод был неверен.** Я отправил 30 запросов на `/api/` — это корневой info-эндпоинт, он не лимитируется by design — и заключил, что лимитера нет вообще. Проверка на эндпоинте, где это реально важно, показывает обратное:

```
$ 20 × POST /api/auth/session/login
401 401 401 ... 401 [15 шт] 429 401 401 401 401

x-ratelimit-bucket: 1677719824298384086
x-ratelimit-limit: 15
x-ratelimit-remaining: 10
x-ratelimit-reset-after: 7509
```

`server/crates/core/ratelimits/` работает: брутфорс логина отсекается на 16-й попытке. Остаётся узкий остаток — сканирование произвольных несуществующих путей не лимитируется, но это дешёвые 404, и добавление `limit_req` на nginx рискует резать легитимные всплески у чат-клиента ради малого выигрыша. **Рекомендация: nginx-лимиты не добавлять**, пункт P2 из плана 07-31 закрыть как неактуальный.

В логах api за 72 часа при этом действительно видна разведка (`/settings`, `/env`, `/config`, `/config.json`, `/v1/config`, `/v1/env`, `/v1/settings`) — все корректно 404.

### M5. `member_fetch_all.rs:43` — panic по запросу (латентный)

```rust
let mut users = User::fetch_many_ids_as_mutuals(db, &user, &user_ids).await?;
members.sort_by(...); users.sort_by(...);
if let Some(true) = options.exclude_offline {
    let mut iter = users.iter();
    members.retain(|_| iter.next().unwrap().online);   // ← :43
}
```

Код предполагает `users.len() == members.len()`. Но `fetch_many_ids_as_mutuals` (`crates/core/database/src/models/users/model.rs:380-394`) маппится по результату `db.fetch_users(ids)` — а `$in`-выборка возвращает **только существующие документы**. Если у члена сервера пропал документ пользователя (удалённый аккаунт, недоведённое удаление), `users` короче `members` → `iter.next()` вернёт `None` → panic.

Триггер — любой аутентифицированный пользователь: `GET /servers/<id>/members?exclude_offline=true`. Rocket ловит panic и отдаёт 500, процесс не падает, но эндпоинт ломается для этого сервера навсегда, пока висит сирота.

**Сейчас не эксплуатируется** — проверил живую БД: 4 member-строки, 3 уникальных пользователя, **0 сирот**. Станет реальным при первом удалении аккаунта.

- [ ] `.retain()` → `zip` по отсортированным спискам либо `while let Some(u) = iter.next()` с graceful-веткой

### M6. Electron 38.1.2 при advisory `<39.8.10` — ✅ бампнут, smoke-тест за вами

`desktop/package.json:40` — `"electron": "38.1.2"` (точный пин). `pnpm audit` в `desktop/`: 105 находок, 1 critical / 57 high. Critical (`node-tar` ← `@electron-forge/cli`) — build-time. А вот сам Electron — рантайм у пользователей, тянет накопленные Chromium/V8 CVE.

- [x] Бампнут до **39.8.10** — минимальная патченая версия (`7a9b70c2`). Намеренно не 43.x: пять мажоров Chromium/Node на раздаваемом пользователям приложении без тест-цикла брать нельзя. Ветка 38.x кончается на 38.8.6, то есть остаться на ней нельзя вовсе.
- [x] Проверено: ставится чисто; eslint — 15 problems / 11 errors **побайтово одинаково** до и после (все преждесуществующие); `tsc` — **0 ошибок в `src/`**
- [ ] **Не проверено: приложение не запускалось и не паковалось ни на одной платформе.** Нужен smoke-тест на Windows/macOS/Linux до релиза. Release-workflow срабатывает только на push в `main`, так что на `dev` бамп может полежать безопасно.

**H7. Сборка desktop для macOS не проходила ни разу — найдено при слиянии в `main` (2026-08-10).** `Desktop Multi-Platform Build` красный во **всех 8 запусках с 2026-04-03**, при этом Windows и Linux всегда зелёные, поэтому на это не смотрели. Две независимые причины, вскрывшиеся одна за другой:

1. **`Cannot find module '../build/Release/volume.node'`.** pnpm 10 блокирует build-скрипты зависимостей, если они не перечислены в `onlyBuiltDependencies`. `fs-xattr` и `macos-alias` (нативные модули, из которых `appdmg` берёт `volume.node`) там не значились — pnpm **писал об этом в лог каждый раз**: `Ignored build scripts: fs-xattr, macos-alias`. Добавлены в список.
2. **`ENOENT: ... 'assets/desktop/icon.icns'`.** `forge.config.ts:50` ссылается на macOS-иконку с момента добавления DMG-мейкера, а файла **никогда не было в репозитории** — в `assets/desktop/` лежат `icon.ico`, `icon.png`, `iconTemplate.png` и `badges/`, `git ls-files | grep icns` пуст. Windows и Linux не замечали, потому что используют `.ico` и `.png`. Сгенерирована из `icon.png` скриптом `desktop/scripts/gen-icns.py` (ни `iconutil`, ни ImageMagick недоступны — контейнер ICNS пишется напрямую); результат проверен обратным разбором: 7 записей, длины сходятся, все PNG читаются.

**Побочная польза для M6:** тот же прогон подтвердил, что **Electron 39.8.10 успешно собирается на Windows и Linux** — сравнение с прогоном 2026-07-31 (Electron 38) даёт идентичную картину, значит бамп ничего не сломал. Остался только запуск приложения руками.

**Найдено позже, когда починилась npm-джоба CI (она падала на клиенте и до desktop не доходила): в `dependencies` было 49 уязвимостей, 1 критическая.** Почти всё — одна перепутанная строка: `@electron-forge/maker-appx` лежал в `dependencies`, тогда как пять его собратьев (deb, dmg, flatpak, squirrel, zip) — в `devDependencies`. Упаковщик тянул `@electron/rebuild` и остальной build-тулчейн в поставляемое дерево, включая ту самую критическую `node-tar`. Перенос оставил 8 настоящих рантайм-зависимостей и снял 49 → 7. Остаток закрыт pnpm-overrides (`conf>ajv`, `ajv>fast-uri`, `discord-rpc>ws`), **привязанными к родителю**: широкий `">="` увёл fast-uri на 4.x и ws на 8.x, и eslint умер на своём ajv. С привязкой дерево встало на fast-uri 3.1.5, ajv 8.20.0, ws 7.5.13. `pnpm audit --prod` → **0**.

**Побочно найдено в desktop:**
- `typescript` запинен на **4.5.5** (2021) при `@types/node` 24.5.2 → `tsc` выдаёт **1619 ошибок разбора внутри `node_modules`** и как проверка бесполезен. Реальная сборка идёт через vite/esbuild, который типы не проверяет, — поэтому этого никто не замечал. Это тот самый пункт P2 из плана 07-31; поднять TS до 5.x означает сделать typecheck осмысленным.
- `tests/ipc-validation.test.ts` и `vitest.config.ts` импортируют `vitest`, которого нет в зависимостях — мёртвая тестовая обвязка, даёт 2 из 11 ошибок линта.

### M7. `mdast-util-to-hast` 13.2.0 — unsanitized class attribute

`client/packages/client/package.json:110`. GHSA-4fh9-h7wg-q85m, fixed **13.2.1**. Путь — `packages__client>mdast-util-to-hast`, то есть рендер markdown, достижимый **из любого сообщения**. Диапазон `^13.2.0` уже разрешает патч — фикс это `pnpm update mdast-util-to-hast` и пересборка web.

Остальное из `pnpm audit --prod` по клиенту (7 находок, 2 high) — обе high это `js-yaml` под `@lingui/conf`, build-time, как и в прошлый аудит. Полный `pnpm audit` даёт 129 находок, но всё остальное — dev-зависимости (eslint/vitest/vite/rollup/babel/postcss/svgo).

---

## LOW

- **H6. Сборка web не была герметичной — в образ утекали `node_modules` с хоста** (найдено при работе над M3/M7, повышено до HIGH: ломает воспроизводимость деплоя клиента).
  `client/.dockerignore:1` содержал `node_modules`. Docker **не** применяет голые паттерны рекурсивно — это исключало только корневой каталог, а все `packages/*/node_modules` продолжали уезжать в контекст. `client/Dockerfile:33` (`COPY packages/ packages/`) выполняется **после** `pnpm install` (строка 28) и клал хостовую symlink-ферму поверх той, что pnpm только что построил внутри образа.
  Сборка проходила только пока состояние `node_modules` на хосте случайно совпадало с локом. Достаточно один раз запустить pnpm на хосте — и следующая сборка с нуля падает: сначала `Cannot find module '/build/packages/stoat.js/node_modules/typescript/bin/tsc'`, после — то же про `rimraf/dist/esm/bin.mjs`. Ровно это и произошло: успешная сборка 2026-08-01 держалась на кэше слоёв Docker, недельный prune его стёр, а мои прогоны pnpm сдвинули хостовые `node_modules`.
  **Диагностика заняла три полных пересборки** — сначала я ошибочно списал поломку на свой бамп `mdast-util-to-hast`, откатил его, и сборка упала снова уже на другом пакете; только это и показало, что причина не в зависимостях.
  Исправлено: `**/node_modules`. Побочный эффект — M7 (бамп mdast) откачен: любое изменение диапазона в package.json заставляет pnpm пере-резолвить лок, а это отдельная задача, завязанная на L0.
- **L0. Три разные версии pnpm в одном проекте** (найдено при работе над M7). `client/package.json:21` пинит `packageManager: pnpm@10.10.0`; `client/Dockerfile:9` делает `corepack prepare pnpm@10.28.1 --activate`; `pnpm install` в образе идёт **без** `--frozen-lockfile`, то есть лок в репо для сборки не авторитетен. Практическое последствие поймано вживую: `pnpm update` одного пакета, выполненный локально (corepack подставил 10.10.0 из `packageManager`), переписал **6008 строк** `pnpm-lock.yaml`, и сборка под 10.28.1 после этого упала — `Cannot find module '/build/packages/stoat.js/node_modules/typescript/bin/tsc'`. Свести к одной версии и перейти на `--frozen-lockfile`, иначе лок в репо остаётся декоративным, а любой локальный `pnpm update` — миной.
- **L1. ✅ Закрыто 08-09 — и по дороге вскрылась ошибка самого аудита.** `pids_limit` действительно не был задан нигде. Но утверждение «лимиты CPU реально применены» оказалось **неверным**: `cpus` в `compose.yml` не было **никогда** (`git log -S "cpus:"` пуст), при этом у всех контейнеров NanoCpus стоял. Проверено решающим тестом — `docker compose up -d --force-recreate --no-deps caddy`: NanoCpus обнулился, лимит памяти (который в файле **есть**) уцелел. То есть CPU-лимиты жили только на долгоживущих контейнерах и тихо терялись при каждом пересоздании; мой первичный вывод описывал контейнеры, а не конфигурацию.
  Исправлено (`558374ac`): значения CPU с контейнеров от 2026-08-01 записаны в файл, рядом добавлен `pids`. **512** — примерно 7× от самого нагруженного сервиса в простое (замер: database 72, rabbit 53, events 37, api 36, остальные <30). Лимит считает **потоки**, поэтому livekit/egress/ingress получили 4096, а voice-ingress 1024 — Chrome и GStreamer под записью плодят их свободно. Объявлено внутри `deploy.resources.limits` рядом с памятью, потому что верхнеуровневый `pids_limit` Compose отвергает как конфликтующий дубль.
  **Побочно вскрылось: стек не поднимается вхолодную.** При полном `docker compose up` api/events пересоздаются вместе с mongo/redis/rabbit, окно `start_period: 30s` закрывалось раньше, чем они успевали подключиться, они помечались unhealthy и роняли caddy (`dependency failed to start: container plg-voice-api-1 is unhealthy`) — **сайт остался недоступен, пока caddy не был поднят руками**. Поймано вживую в ходе этой же правки. `start_period` поднят до 90s.
- **L2.** Нет healthcheck у `bot`, `crond`, `pushd`, `voice-ingress`, `livekit-ingress`, `livekit-egress`, `createbuckets`. Для distroless-образов (crond/pushd/voice-ingress) это принципиально невозможно через CMD-SHELL, но `bot` (порт 3939) и livekit-ingress/egress — не distroless, им healthcheck сделать можно.
- **L3.** ✅ **Закрыто 2026-08-09.** `plans/audit-2026-07-31.md:16,23` содержал префикс утёкшего ключа шифрования файлов в публичном репо. Ключ и так считается disclosed, но фрагменты секретов в документации — плохая привычка и вечный false positive для сканеров. Отредактировано до push'а H1.
- **L4.** `server/deny.toml:163` — `wildcards = "allow"` (открыто с прошлого раза).
- **L5.** Docker: 38.96 ГБ реклеймится (91% от 42.69 ГБ образов). Недельный cron (`0 4 * * 0 docker system prune -af --filter until=168h`) это подберёт; на `/` свободно 65 ГБ.
- **L6.** `desktop`: TypeScript `~4.5.4`, `tsconfig` без `strict`, ESLint 8 против ESLint 9 в клиенте — без изменений с прошлого аудита.
- **L7.** Бот: `bot/index.js` — 2954 строки, **0 тестов** (пункт C5 прошлого плана; линтер добавлен `42b5eae1`, тесты нет). H3 — прямая иллюстрация того, чем это оборачивается.
- **L8.** Responsive: с прошлого аудита появились `@media` в 6 файлах и 33 использования Panda-брейкпоинтов (`md:`/`sm:`/`lg:`) — сдвиг с «2 файла, 0 брейкпоинтов». `breakpoints` в `panda.config.ts` по-прежнему не объявлены, но Panda использует дефолтные `sm/md/lg/xl/2xl`, так что это работает.

---

## Что сделано 2026-08-09

Работа шла в тот же день, что и аудит. Коммиты на `dev`, запушены.

| Коммит | Что |
|---|---|
| `77c4f2b6` | L3 — вычищен префикс утёкшего ключа из плана 07-31 (**до** push'а H1, чтобы не отправлять его в публичный репо) |
| `06ff3545` | Отчёт по аудиту |
| — | **H1**: 21 коммит + ветка-чекпоинт запушены, `push.autoSetupRemote` включён. CI впервые прогнался на этой работе |
| `7a6b6a76` | **H3**: права бота — реальный расчёт вместо «есть роль», 11 тестов (первые в боте), CI их гоняет |
| `be0c554c` | Хвост H3: `permissions.js` не попадал в образ → бот падал в crash-loop. Поймано сразу, потому что `uncaughtException` теперь делает `exit(1)` (543bd9be) |
| `172fcb95` | **H2**: jxl-oxide 0.12.6 / jxl-modular 0.11.3 / jxl-grid 0.6.2; убран хардкод 0.8.1 в autumn → минус 9 крейтов из лока, `cargo check` чист, OSV по 13 jxl-крейтам — 0 |
| `0592747e` | **M1**: nginx развёрнут, заголовки в одном владельце, починена потеря заголовков на всей статике |
| `7b725e88` | **M3**: `sourcemap: false` |
| `fe943ab0` | **H4**: `cargo audit` + `pnpm audit` в CI; **H2-хвост**: `Dockerfile.autumn` + локальная сборка autumn |
| `690d9636` | **H6**: `.dockerignore` → `**/node_modules`, сборка web стала герметичной; M7 откачен |
| `7a9b70c2` | **M6**: Electron 39.8.10 |

| `df6dd0f8` | **M5**: сопоставление member↔user по id вместо позиции |
| — | **H2 развёрнут**: `autumn` пересобран локально и запущен. В новом бинаре jxl-oxide **0.12.6**, jxl-modular **0.11.3**, jxl-grid **0.6.2** (сверено `strings`) |

**Проверка H2 после замены образа autumn** (точка отката `plg-voice-autumn:rollback-20260809`):
- два реальных вложения скачаны **байт-в-байт идентично** тому, что отдавал upstream-образ → дешифрование (`FILES_ENCRYPTION_KEY`) и перекодирование в WebP не изменились
- загружен `tests/assets/dice.jxl` → HTTP 200, autumn извлёк `type: Image, width: 800, height: 600` — ровно то, что утверждает тест `media_impl.rs:266`. То есть пропатченный декодер отработал на живом jxl-пути.
- в логах autumn ни ошибок, ни паник. Тестовое вложение остаётся неприкреплённым и уберётся задачей `prune_dangling_files` в crond.

**Проверено в проде:** бот жив с новой логикой прав (владелец/Admin → true, косметическая роль/без ролей → false, на живом API); 7 security-заголовков по одному на HTML и впервые на `/assets/*.js`; `.map` отдаёт 404 вместо 11.5 МБ, dist 53.8 → 34.3 МБ, клиент рендерит; `/`, `/api/`, `/autumn/`, `/january/` → 200, `/stream/active` → 401; CI: итог `success`, secrets-scan и bot-tests зелёные, advisory-джобы отчитались.

**Три поправки к самому аудиту** (все внесены выше): M4 был ошибкой — лимитер работает; H2 не доезжает до прода без локальной сборки autumn; в ходе работ найдены H5 и H6, которых в первичном отчёте не было.

---

## Что осталось

**Требует вас — я это сделать не могу:**

- **H5. `ghcr.io` недоступен.** Прокси `147.90.15.214` имеет вхосты для `github.com` и `registry-1.docker.io`, но не для `ghcr.io`. Пять сервисов (`january`, `crond`, `pushd`, `voice-ingress`, `livekit`) невозможно перетянуть — они живы только на локальных образах. `autumn` с ghcr.io уже снят, остальные ждут. Машина не эта.
- **M6. Electron 39.8.10 не проверен запуском.** Ставится чисто, typecheck 0 ошибок, линт не изменился — но приложение ни разу не стартовало и не паковалось. Нужен smoke-тест на Windows/macOS/Linux до релиза. На `dev` бамп лежит безопасно: release-workflow срабатывает только на `main`.

**Открыто, но не срочно:**

- **L5.** Docker: ~39 ГБ реклеймится; недельный cron это подберёт, на `/` 65 ГБ свободно.
- **L7.** `bot/index.js` — 2954 строки, `COMMANDS` одним литералом. Тесты у бота теперь есть (11 штук, `permissions.js`), но покрыты только права; XP-формула, автомод и парсеры команд — нет.
- **L8.** Responsive: 6 файлов с `@media`, 33 использования Panda-брейкпоинтов. `breakpoints` в `panda.config.ts` не объявлены, но Panda использует дефолтные `sm/md/lg/xl/2xl`, так что это работает.
- **C4 из плана 07-31** — миграция `async-std → tokio`. Из неё же уйдёт `tokio 0.2` (RUSTSEC-2021-0124), единственная запись в `audit.toml`, отложенная не по причине недостижимости.
- **CSP** всё ещё разрешает `unsafe-inline`/`unsafe-eval` — известный долг, переход на nonce.

---

*Отчёт по фактам, собранным на живом хосте 2026-08-09. Проверки, которые оказались ложными и не попали в находки: TLS 1.0/1.1 (первый тест дал false positive из-за того, что openssl печатает `Protocol :` в блоке сессии и при неудачном handshake — при строгой проверке обе версии отклоняются); orphan-члены в БД (0).*
